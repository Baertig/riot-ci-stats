services:
  crawler:
    build:
      context: ..
      dockerfile: crawler/Dockerfile

    volumes:
      - .:/data

    command: ["python", "fetch_stats.py", "-f", "/data/ci-stats.duckdb"]
    
      